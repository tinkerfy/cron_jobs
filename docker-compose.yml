services:
  mysql:
    image: mysql:8.0
    container_name: cronjobs-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: postgress
      MYSQL_DATABASE: cronjobs
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
